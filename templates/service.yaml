apiVersion: v1
kind: Service
metadata:
  name: {{ include "pg-zalando.fullname" . }}
  labels:
    {{- include "pg-zalando.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pg-zalando.selectorLabels" . | nindent 4 }}
